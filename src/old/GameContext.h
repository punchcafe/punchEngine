//

class GameContext{
  ForceField forceField;
  SpriteManager spriteManager;
  SceneManager sceneManager; //Takes care of loading a scene into a forceField as the player progresses.
  SceneLoader sceneLoader; //Takes care of loading a scene from resources.
}
